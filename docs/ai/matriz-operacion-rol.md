# Matriz operación → rol

**Las operaciones REST del backend v2, con quién puede llamarlas, dónde se decide qué ve y **qué
autoridad lo decide**.**

> **Aquí ya no va ninguna cifra de estado.** Decía «190 operaciones», y antes «189», y antes «180»
> cuando la tabla tenía 187 filas: **una cifra transcrita a mano caduca sola y nadie la revisa,
> porque nada la verifica**. Desde el 2026-09-02 el recuento lo **imprime el gate**
> (`MatrizOperacionRolTest`) en cada build, con el desglose por rol y por autoridad, y **ese censo
> es el número**. Si necesitas verlo sin compilar:
>
> ```sh
> grep -cE '^\| (GET|POST|PUT|DELETE|PATCH) \|' docs/ai/matriz-operacion-rol.md
> ```
>
> **Las barras van escapadas, y no es un detalle de estilo.** El comando que estuvo escrito aquí
> las llevaba sin escapar; dentro de una expresión regular extendida `|` es *alternación*, así que
> `'^| … |'` se lee como «principio de línea **o** …», que casa con **todas** las líneas del
> fichero. Devolvía **345** —el total de líneas, medido el 2026-08-30— en vez del recuento real. Un
> comando de verificación que no verifica es peor que ninguno: da un número, y por eso nadie lo
> vuelve a mirar.

---

## El token `{autoridad: …}`

**Cada fila con gate de rol termina su celda de Alcance con exactamente un token
`{autoridad: CLAVE}`**, y la clave sale de un **vocabulario cerrado** que declara
`MatrizOperacionRolTest`. El token dice **qué comprueba de verdad el Core** sobre el objeto de esa
fila — no lo que debería comprobar: si sólo decide la anotación del controlador, la clave es
`BANDA`, y eso se escribe tal cual.

**Dos formas de clave.** Las escritas `Clase.metodo` nombran el método que decide, y el gate
comprueba **contra el bytecode** que esa clase existe en `com.controllocal.service` y declara ese
método; las cuatro en mayúsculas —`TENANT`, `BANDA`, `SESION`, `GOBIERNO`— dicen que **no hay
componente de autoridad**: sólo la frontera de organización, sólo la anotación, sólo la propia
sesión, o sólo el gobierno del tenant (y `GOBIERNO` únicamente en filas cuyo rol es exactamente
`TENANT_ADMIN`).

**En las filas `TODOS` y `PUBLICO` el token es opcional** y hoy lo llevan dos, las dos lecturas
donde la autoridad decide *cuánto* se ve. Existe para que la prosa de esta columna deje de poder
mentir: hasta aquí `POST /locales/{id}/precios` declaró «un local de sus captaciones» mientras el
código sólo miraba el tenant, y ningún gate lo veía (`N36`).

Este documento es la **fuente de verdad**, no un resumen: `MatrizOperacionRolTest`
(`controllocal-app/src/test/java/com/controllocal/arquitectura/`) lo parsea y **rompe el build**
si el código y la tabla dejan de coincidir. No puede quedar desactualizado, y un endpoint nuevo no
entra sin una fila que declare su decisión de rol.

Está pensado para el SPA Angular: es la entrada directa del estado por rol y de la navegación
(el equivalente de `RouteAccess` del Blazor).

---

## Cómo leer la columna **Roles**

| Valor | Qué significa |
|---|---|
| `PUBLICO` | Sin token. Son siete y están en la lista `permitAll` de `ConfiguracionSeguridad`, que un test compara contra este documento en los dos sentidos. |
| `TODOS` | Autenticado, **sin gate de rol**. Los tres roles entran y lo que cambia es *qué ven*: eso es la columna **Alcance**, que aquí es obligatoria. |
| `AGENTE`, `BROKER`, `TENANT_ADMIN` | Gate explícito (`@PreAuthorize`). Fuera de esos roles: **403** con el mensaje congelado *"No tienes permisos para esta operacion."* |

> **`TENANT_ADMIN` no es el `ADMIN` de antes (Bloque 5, D-S0-17).** Hasta aquí el
> administrador **era un broker con un booleano** (`detalle_broker.es_administrador`) y
> entraba por herencia a 18 operaciones comerciales. Ahora es una **banda propia**, resuelta
> en servidor desde `usuario_organizacion`, bajo la regla *gobernar no es operar*: ve todo su
> tenant **en lectura**, gobierna cuentas y organigrama, y **no firma ningún hecho del
> negocio** — ni aprueba captaciones, ni las cierra, ni conforma documentos, ni evalúa
> solicitudes. De las 18 filas compartidas, **8 cambiaron de dueño**.
>
> **El rol que viaja en el token sigue siendo `ADMIN`** y no aparece en esta tabla: el formato
> está congelado y solo admite tres valores mientras GlassFish conviva (R1). La banda real se
> resuelve por petición y se consulta con `GET /sesion`.
>
> Si una persona gobierna **y** opera, lleva **dos roles explícitos** y la auditoría dice cuál usó.

**Buena parte de las operaciones no lleva gate de rol** (`PUBLICO` + `TODOS`). **Cuántas, lo dice el
censo del gate**, no esta frase: aquí decía «92 (7 + 85)», después «91 (7 + 84)», y las dos
envejecieron por separado del total. El desglose se reproduce sin compilar así:

```sh
grep -E '^\| (GET|POST|PUT|DELETE|PATCH) \|' docs/ai/matriz-operacion-rol.md \
  | awk -F'|' '{print $4}' | sed 's/^ *//; s/ *$//' | sort | uniq -c
```

No es un olvido:
en la v1 el control de esas operaciones es de *alcance*, no de *acceso* — todos entran y cada uno
recibe su porción. Por eso la columna **Alcance** es la parte importante de esta tabla, y la que el
SPA necesita para no prometer en pantalla lo que el backend va a devolver vacío.

Dos gates están declarados **a nivel de clase** y por eso valen para todos los handlers del
recurso: `/agentes` (BROKER, TENANT_ADMIN) y `/asignaciones` (TENANT_ADMIN). En `/agentes` **dos
métodos lo estrechan**: `POST` y `PUT` llevan su propio `@PreAuthorize` de `TENANT_ADMIN`, porque
dar de alta y editar agentes pasó a ser gobierno (filas 17 y 18). La tabla lista siempre los roles
**efectivos**, sin importar dónde esté escrita la anotación.

> **Regla de fondo, anterior al rol**: desde V6 el alcance arranca por el **tenant**. El "todo" de
> un TENANT_ADMIN es todo lo de **su** organización, nunca lo de otra corredora. Un id de otro tenant
> responde **404**, no 403. `Alcances.de(actor)` resuelve las dos capas en ese orden.

---

## F0 — Identidad y salud

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/salud` | PUBLICO | Sin datos de negocio. | F0 |
| POST | `/auth/login` | PUBLICO | Mientras D-20 mantenga la convivencia con GlassFish, solo busca credenciales en el tenant `BROX_LEGACY`. | F0 |
| POST | `/auth/logout` | TODOS | **Aditivo (D-S0-12), no existe en la v1.** Logout con efecto en servidor: sella `sesiones_invalidas_desde` de la credencial del actor y responde 204. Alcance implícito y no discutible — **solo puede cerrar su propia cuenta**, porque la persona sale del token, no del cuerpo. Cierra **todas** las sesiones de esa cuenta: sesiones individuales exigirían un `jti` que no cabe en el token congelado. | F0 |
| POST | `/auth/renovar` | TODOS | **Aditivo, no existe en la v1.** Reemite el token del actor y convierte los 30 minutos en un límite de **inactividad**, no absoluto: quien trabaja sigue dentro, quien deja el navegador quieto media hora cae. Alcance implícito y no discutible — **solo renueva su propia sesión**, porque la identidad sale del token y no del cuerpo. No relaja nada: exige un token que el filtro ya validó (firma, caducidad y revocación por `sesiones_invalidas_desde`), así que **no puede resucitar una sesión revocada**; y **no está en las listas de sesión capada**, de modo que una contraseña temporal o un MFA sin enrolar siguen bloqueando en vez de renovarse indefinidamente. La identidad se relee con `identidadDe` para que un rol cambiado o una cuenta desactivada no sobrevivan otros 30 minutos. | S0 |
| GET | `/aviso-privacidad` | PUBLICO | **Aditivo (D-27), no existe en la v1.** Versión vigente del aviso. Público **a propósito**: el titular debe poder leerlo sin cuenta, y el enlace del formulario de alta apunta aquí. Sin parámetros, sin datos personales y **misma respuesta para todos** — a diferencia de `/documentos/contenido`, cumple de verdad las condiciones de una ruta pública. | F0 |
| POST | `/auth/recuperacion` | PUBLICO | **Aditivo (§4.3), no existe en la v1.** Responde **202 siempre**, exista o no la cuenta y venga vacío el cuerpo: cualquier otra cosa convertiría el endpoint en un padrón de usuarios. Público porque quien lo usa **no tiene sesión** — es lo que viene a recuperar. Consume cupo del bloqueo **solo por IP**: contarlo por cuenta dejaría bloquear la cuenta ajena pidiendo su recuperación en bucle, que sería una denegación de servicio dirigida y gratuita. | S0 |
| POST | `/auth/recuperacion/canje` | PUBLICO | **Aditivo (§4.3).** Canjea el token de un solo uso —sirve para `RECUPERACION` y para `INVITACION`— y el **titular** define su clave. `usado_en` se sella en la misma transacción, así que repetirlo no hace nada. Caducado, usado, reemplazado o inventado dan **el mismo error**. | S0 |
| POST | `/perfil/contrasena` | TODOS | **Aditivo (§4.2), no existe en la v1** (H-02: el `PUT` de brokers y agentes **ignoraba** el campo). Exige contraseña **actual** + nueva. Alcance implícito y no discutible: **solo sobre su propia cuenta**, porque la persona sale del token. Responde 204 e **invalida todas las sesiones, incluida la que llama**. | S0 |
| GET | `/sesion` | TODOS | **Aditivo (Bloque 5, R3), no existe en la v1.** Publica la **banda efectiva** del actor (`AGENTE`/`BROKER`/`TENANT_ADMIN`), que **no cabe en el token**: su formato solo admite tres valores y el `ADMIN` que lleva es la banda heredada. Alcance implícito y no discutible — **solo habla de quien pregunta**, porque todo sale de la sesión y no acepta ni un parámetro. Sin gate a propósito: negarle a alguien saber quién es sería absurdo. | S0 |
| POST | `/auth/mfa/desafio` | PUBLICO | **Aditivo (V37, D-S0-22).** Primer paso del login con segundo factor. Responde **200 + el `LoginResponse` congelado** si la cuenta no tiene MFA y **202 + un desafío** si lo tiene, de modo que el cliente usa **un solo camino** sin adivinar lo que no puede saber. Público porque es PRE-sesión por definición; consume el mismo cupo por cuenta e IP que el login. | S0 |
| POST | `/auth/mfa/verificar` | PUBLICO | **Aditivo (V37).** Canjea el desafío por la sesión, con un código TOTP **o** uno de respaldo — el servidor decide cuál es sin que el cliente lo declare. El desafío es su única credencial, muere al primer uso y **no autoriza ningún otro endpoint**. Tres controles de intentos a la vez: 5 por desafío, acumulado por cuenta **que no se reinicia pidiendo desafíos nuevos**, y por IP. | S0 |
| GET | `/perfil/mfa` | TODOS | **Aditivo (V37).** Estado del propio factor y cuántos códigos de respaldo quedan. **Nunca devuelve el secreto**: no existe endpoint que lo relea. Alcance implícito y no discutible — sale de la sesión, así que solo habla de quien pregunta. | S0 |
| POST | `/perfil/mfa` | TODOS | **Aditivo (V37).** Inicia el enrolamiento y devuelve secreto y `otpauth://` **una sola vez**, con `Cache-Control: no-store`. El factor nace PENDIENTE y **caduca a los 15 minutos** si no se confirma. | S0 |
| POST | `/perfil/mfa/confirmar` | TODOS | **Aditivo (V37).** Activa el factor con el primer código y devuelve los **8 códigos de respaldo**, también una sola vez. Cuatro efectos inseparables: ACTIVO, códigos, apagar `debe_enrolar_mfa` e **invalidar las sesiones vivas** —nacieron sin segundo factor—. Si es el primer administrador, la organización cruza a MFA de gobierno. | S0 |
| DELETE | `/perfil/mfa` | TODOS | **Aditivo (V37, D-S0-34).** Revoca el factor propio. Exige **contraseña + código vigente**: una sesión abierta no basta, porque si bastara, robarla equivaldría a quedarse con la cuenta. Se rechaza si dejaría al tenant **sin administrador operativo**. | S0 |
| POST | `/perfil/mfa/codigos` | TODOS | **Aditivo (V37).** Regenera los códigos e **invalida todos los anteriores**, usados o no. Exige contraseña + código vigente. **No** invalida sesiones: regenerar códigos no cambia quién eres ni cómo entras. | S0 |
| POST | `/perfil/elevacion` | TODOS | **Aditivo (V37, D-S0-34).** Token de elevación de **5 minutos**, un solo uso, ligado a credencial + tenant + **acción concreta**. Existe porque el token congelado **no lleva** cuándo se probó el segundo factor: inferirlo de una sesión nacida hace horas sería falso. Viaja en cabecera, nunca en la URL. | S0 |
| DELETE | `/accesos/{idPersona}/mfa` | TENANT_ADMIN | **Aditivo (V37).** Nivel 2 de la recuperación: el gobierno **revoca** el factor de otra persona; no lo ve ni lo fija, y el titular vuelve a enrolarlo. Un BROKER **no** revoca el de nadie, ni el de sus agentes (D-S0-18). Exige **elevación** y **motivo obligatorio**, e invalida las sesiones del afectado. Contra sí mismo se rechaza; si el afectado es el **último administrador operativo**, también — esa es del nivel 3. Otra organización responde **404, no 403**. {autoridad: GOBIERNO} | S0 |
| GET | `/accesos` | TENANT_ADMIN | **Aditivo (V37).** Padrón de cuentas del **propio** tenant: quién tiene acceso, con qué banda, si su cuenta está activa y si tiene segundo factor. Existe porque las fichas comerciales congeladas identifican por `persona_rol.id` y **todo este recurso habla de la persona**; publicar aquí la correspondencia evita añadir `idPersona` a `AgenteResponse` y `BrokerResponse`, que están congelados. Devuelve **cuántos** códigos de respaldo quedan, nunca los códigos ni el secreto. {autoridad: GOBIERNO} | S0 |
| POST | `/accesos/{idPersona}/invitacion` | TENANT_ADMIN | **Aditivo (§4.4, D-S0-18).** Gobierno del tenant: un BROKER **no invita**, ni a su propio equipo. Devuelve el token **una sola vez** (en la base solo queda su hash) e invalida la invitación anterior de esa cuenta. Alcance: personas del **mismo tenant**; otra organización responde **404, no 403** — un 403 confirmaría que esa persona existe. {autoridad: GOBIERNO} | S0 |
| POST | `/accesos/{idPersona}/contrasena-temporal` | TENANT_ADMIN | **Aditivo (§4.4).** La contraseña la **genera el sistema**, no la elige el administrador, y se muestra una sola vez. Deja la cuenta con `debe_cambiar_contrasena` e invalida sus sesiones vivas. Mismo alcance y mismo 404 que la invitación. {autoridad: GOBIERNO} | S0 |
| — | `/gestion/recuperacion/**` | **FUERA DE ESTA MATRIZ** | **Aditivo (V38).** Recuperación de emergencia. **No es API del producto**: vive en `com.controllocal.web.gestion` —fuera del paquete que este gate escanea— y solo se atiende en el conector ligado a `127.0.0.1`, en un puerto que Docker no publica; pedirla por el puerto público responde **404**. No lleva rol porque **no hay sesión**: quien la usa es un operador local con dos aprobaciones de custodio, no un usuario. No emite token. Apagada por defecto. | S0 |
| GET | `/seguridad/avisos` | TENANT_ADMIN | **Aditivo (V37, §11 · D-S0-49).** Aviso persistente de gobierno: quién tocó factores, accesos o roles en **su propia organización**. Lectura sobre `evento_seguridad`, que es append-only y de un solo escritor — por eso **no se puede atender ni silenciar**, que es justo lo que haría quien acabara de revocar un factor sin permiso. Se filtra a los hechos de gobierno: un tablero ahogado en `LOGIN_OK` deja de avisar. **No publica** `detalle_json` ni el agente de usuario. `Cache-Control: no-store`. {autoridad: GOBIERNO} | S0 |

## F2 — Oferta (locales comerciales)

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/documentos/contenido` | TODOS | **Dejó de ser público el 2026-08-08 (H-12 cerrado).** Era `PUBLICO` replicando la v1, con el argumento de que la clave era una *capability*; no lo era —es la ruta física, filtra el correlativo y el nombre, tiene 32 bits, no caduca ni se revoca y viaja en el query string, o sea en los access logs—. Por ahí se descargan documentos de identidad. Con el Blazor eliminado desapareció hasta la excusa: el SPA siempre mandó el token. Alcance: la clave sigue siendo lo único que identifica el binario, así que quien tenga sesión y clave lee — acotarlo por dueño es trabajo aparte. | F2 |
| GET | `/locales` | TODOS | Cartera completa de la organización; no se acota por rol. Filtra (`texto`, `estado`), ordena, pagina y cuenta **en SQL**; los filtros son aditivos y opcionales. **Desde 2026-09-02 la búsqueda la resuelve `MotorBusquedaInmobiliaria`, el mismo que sirve a `/propiedades`**: una sola estrategia (conjunto de candidatos, RC-003) configurada por recurso. Lo propio de éste es que ordena `id` **ascendente** y que no tiene filtros de inmueble; el texto busca **lo mismo** en los dos, rubro incluido. Y la validación común de los filtros es la misma para los dos: un `estado` fuera de `D`/`N`/`I` es **400** —antes era una página vacía— y un `page` no numérico es **400**, no 500. | F2 |
| GET | `/locales/resumen` | TODOS | Mismo alcance que el listado (tenant, sin rol) y el mismo filtro `texto`, para que los KPI cuadren con la lista. Se calcula con un `group by` en la BD, **no** contando filas descargadas. | F2 |
| GET | `/locales/{id}` | TODOS | Cartera de la organización. | F2 |
| GET | `/locales/mis-locales` | AGENTE | Los locales de **sus captaciones** (RF-004), no los que registró. {autoridad: LocalComercialServiceImpl.listarMisLocales} | F2 |
| POST | `/locales/posibles-duplicados` | AGENTE | Advertencia no bloqueante dentro del tenant: compara inmuebles del mismo propietario por dirección técnica, unidad/piso compatibles y metraje aproximado. {autoridad: TENANT} | F2 |
| DELETE | `/locales/{id}` | AGENTE | Baja lógica (estado I); **sólo el `id_rol_responsable` de la propiedad** (P0-1, V87). Sustituye a `exigirPertenencia`, que era un OR de «captación viva ∨ prospección ∨ `id_rol_incorporo`»: con dos encargos de agentes distintos daba verdadero para los dos, y convertía una procedencia histórica e inmutable en un permiso vigente. Si nunca estuvo en oferta, la baja **no** declara un retiro comercial. Desde F2.10 la fila de `propiedad` se **toma al cargarla**, así que el responsable que se comprueba es el que seguirá siéndolo cuando la baja escriba. {autoridad: AutoridadDePropiedad.exigirEdicion} | F2 |
| GET | `/locales/{id}/precios` | TODOS | **La frase anterior era falsa y describía una protección que no existía.** Decía «colección hija: se alcanza por el id del padre, que sí va filtrado por tenant» — y el padre **no se cargaba**: el servicio consultaba `precio_propiedad` por `id_propiedad` sin actor y sin `organizacion_id`, así que cualquier usuario autenticado de **otra corredora** leía la serie económica completa de cualquier propiedad por su id. Corregido el 2026-09-01. Ahora, en este orden: (1) la propiedad se carga por `(organización, id)` y un id ajeno responde **404** —no una lista vacía, que afirmaría que existe y no tiene precios—; (2) se aplica **D-P0-6 hito a hito**, porque los hitos de una propiedad no son todos del mismo sujeto: uno con `id_captacion` pertenece al histórico de **ese encargo** y lo lee su agente o el BROKER que lo supervisa hoy; uno **sin** encargo (legado) no tiene episodio al que atribuirse y sigue la regla de la **PROPIEDAD** —su responsable, o el bróker que lo alcanza—, sin inventarle un encargo para poder clasificarlo. El **TENANT_ADMIN** la recibe **vacía**: gobernar no es operar, y lo que se pidió y lo que se cerró son información comercial. La respuesta puede quedar vacía, y vacía significa «de esta serie no te corresponde ningún hito». {autoridad: AutoridadDePropiedad.puedeLeerHistoricoDelEncargo} | F2 |
| POST | `/locales/{id}/precios` | AGENTE | Hito del histórico **del encargo que lo autorizó, y sólo si ese encargo es del actor** (P0-4, V87). Hasta V87 esta fila declaraba «un local de sus captaciones» y el código sólo comprobaba el tenant: cualquier agente metía una cifra en la serie económica de un encargo ajeno. La operación se declara o se deduce del único encargo vivo (D-E4-1), y la autoridad se exige **después** de resolverlo, porque hasta ahí no se sabe de qué encargo es el importe. Desde F2.10 las filas de los encargos **vivos** de la propiedad se toman antes de resolver cuál toca —en orden de id, y son como mucho dos—, para que la autoridad decida sobre el agente que seguirá siéndolo cuando el hito se escriba. {autoridad: AutoridadDePropiedad.exigirEdicionDelEncargo} | F2 |
| GET | `/locales/{id}/fotos` | TODOS | Colección hija del local. | F2 |
| POST | `/locales/{id}/fotos` | AGENTE | Máximo 6 por local; el binario ya está en el almacén. **Ya no es «cualquier agente del tenant»** (P0-1, V87): las fotos **son** la ficha del inmueble, así que las escribe **su `id_rol_responsable`** vía `AutoridadDePropiedad.exigirEdicion`. Una propiedad FALTANTE no admite fotos de nadie hasta que un broker asigne. La ficha (`GET /locales/{id}`) viaja con `responsabilidad` resuelta para que la pantalla **no deduzca** la regla. Desde F2.10 la fila de `propiedad` se **toma al cargarla**: la foto se inserta bajo el mismo responsable que autorizó la escritura. {autoridad: AutoridadDePropiedad.exigirEdicion} | F2 |
| DELETE | `/locales/{id}/fotos/{idFoto}` | AGENTE | Devuelve la clave para limpiar el almacén. Misma autoridad que el alta y **por el id de la propiedad, no por el de la foto** (P0-1, V87): la foto no tiene dueño propio, y llegar por su id no puede ser una puerta más barata que llegar por la del inmueble. Desde F2.10 la fila de `propiedad` se **toma al cargarla**, porque borrar es la escritura más irreversible de la ficha. {autoridad: AutoridadDePropiedad.exigirEdicion} | F2 |

## E4 — Propiedad universal y captura

**El modelo nuevo (D-E4-1, D-E4-2).** `/locales` sigue siendo el alta de la v1 —un local comercial
en alquiler, un propietario, el precio en una columna— y no se toca. `/propiedades` es el modelo
entero: siete tipos, copropiedad con cuotas, atributos gobernados por catálogo y **una o dos
operaciones simultáneas**, cada una con su encargo, su precio y su histórico.

`/captura` es el motor de preguntas, y lo consumen **Angular y KAIROS por igual**: qué se sabe, qué
falta, qué se pregunta ahora y si ya hay suficiente para ejecutar. Ninguno de los dos clientes
puede tener una segunda copia de esas reglas — salen del catálogo.

Dos cabeceras cruzan todo el bloque. `Idempotency-Key` hace que un reintento devuelva lo que
produjo el primer intento en vez de duplicar la propiedad; `X-Origen` (UI, KAIROS, API, SISTEMA)
viaja al evento de dominio y es lo que responde *«quién decidió esto»*.

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| POST | `/propiedades` | AGENTE | Alta universal en la organización del actor, en **una sola transacción**. `operaciones` admite **cero, una o dos**. Con cero crea sólo lo que pertenece a la propiedad —propiedad, ubicación, titulares, atributos y evento— y **nada comercial**: ni encargo, ni condición económica, ni hito `U`, ni precio, ni disponibilidad; queda registrada y **no ofrecida** (V75). Con una o dos abre además su encargo por operación, con condición económica y primer hito `U`, y la pone en oferta. El encargo nace PENDIENTE — el agente registra, el broker decide. **Y el agente que la registra queda como su responsable inicial** (P0-5, `V88`), con su fila `origen=ALTA` en `asignacion_responsable_propiedad`: una sola por propiedad, garantizado por índice. Vale **sólo** cuando nace una fila de `propiedad` — retomar o volver a encargar una existente **no** cambia su responsable. {autoridad: AutoridadDePropiedad.fijarAlAlta} | E4 |
| GET | `/propiedades` | TODOS | Cartera de la organización, con **una fila por propiedad y sus encargos vivos dentro** — no un precio suelto sin operación, como el listado heredado. Filtra por `texto`, `tipoPropiedad`, `distrito`, `estado` y `operaciones`, todo en SQL antes del LIMIT. `operaciones=VENTA,ALQUILER` significa «tiene las dos vivas», y se resuelve con dos EXISTS porque no existe ningún valor combinado que consultar. Sin alcance por rol: la cartera es del tenant, igual que `/locales`. **Desde 2026-09-02 la búsqueda la resuelve `MotorBusquedaInmobiliaria`, el mismo que sirve a `/locales`**: hasta entonces este recurso llevaba una consulta paralela —un solo `OR` cruzando tres tablas— que no alcanzaba ningún índice trigrama. Lo propio de éste es que ordena `id` **descendente** y que filtra por tipo, distrito y operaciones. El texto busca **lo mismo** que en `/locales`, **rubro incluido** (C0-a): `rubro_permitido` es atributo gobernado de PROPIEDAD desde V71, así que su rama es canónica; que sólo **aplique** a ALMACEN, LOCAL y OFICINA no la excluye —los otros cuatro tipos simplemente no producen candidatos por ella, y la base rechaza escribirles un rubro. La validación común es la misma para los dos: `estado` fuera de `D`/`N`/`I` es **400** y `page` no numérico es **400**. | E4 |
| POST | `/propiedades/{id}/observaciones` | AGENTE | **Lo que se vio del mercado** sobre este inmueble (V76): fecha observada, operación, importe, moneda y **fuente**, todas obligatorias. No es un hecho comercial de BROX —no lo autorizó, no lo publicó y no lo negoció—, así que **no** escribe `precio_propiedad`, ni el precio de la propiedad, ni la disponibilidad. Serie **append-only**: no hay PUT ni DELETE, y su ausencia es la regla — si el precio cambió, se observa otra vez. {autoridad: TENANT} | E4 |
| GET | `/propiedades/{id}/observaciones` | TODOS | Lo observado de una propiedad, de lo más reciente a lo más antiguo, con su fuente y quién la capturó. Mismo alcance que la ficha. | E4 |
| GET | `/propiedades/filtros` | TODOS | Los valores que el filtro puede ofrecer sin inventarse ninguno: hoy los distritos **con cartera** del tenant. Los tipos y las operaciones no están aquí — son vocabulario del dominio y los publica `/captura/apertura`, que es su único dueño. | E4 |
| GET | `/propiedades/{id}` | TODOS | Cartera de la organización; un id de otro tenant responde **404**. **El read model completo de la ficha**, listo para pintarse: la cosa física (tipo, ubicación, atributos con su rótulo, titulares con cuota y representante), **todos** sus encargos —vivos y cerrados, cada uno con su importe, su agente, su exclusividad y su histórico **separado por encargo**, no por operación— y su **actividad** (oportunidades, visitas, interacciones, expedientes y contratos), donde **cada hecho lleva el `idEncargo` del que nace**. Cada código viaja con su rótulo: el cliente no traduce ninguno. **Y desde el 2026-09-01 la información comercial HISTÓRICA se acota por actor (D-P0-6)**, sin tocar el resto de la ficha: `encargos[].historico` llega **nulo** cuando quien pregunta no puede leer ese encargo —lo leen su propio agente y el BROKER que lo supervisa hoy—, `historia` llega nula si no puede leer la historia de la propiedad —la leen el AGENTE **responsable** y el BROKER que lo alcanza (`alcanzaIncluidoSinDueno`, el mismo predicado de C5)— y `actividad` sólo trae los hechos de sus **encargos visibles**. El **TENANT_ADMIN** no recibe ninguno de los tres: **gobernar no es operar**, y poder decidir quién responde no concede los importes ni los cierres; quien gobierne **y** opere lo obtiene actuando con su banda BROKER. **Y cuando sí puede, la `historia` se compone SÓLO de los encargos que ese actor ve**: un bloque agregado no puede convertirse en la puerta por la que se lee el importe de un encargo ajeno. Lo que **no** se acota: la cosa física, los titulares, los atributos, los importes **vigentes** de cada encargo, sus condiciones, sus anuncios, `puedeEditar` y `responsabilidad`. **Nulo no es vacío**: Jackson va `NON_NULL`, así que el campo **no viaja**, y el SPA debe leer su ausencia como «no disponible para ti» y no como «no ha pasado nada». {autoridad: AutoridadDePropiedad.puedeLeerHistoricoDelEncargo} | E4 |
| PUT | `/propiedades/{id}` | AGENTE | Edición parcial: lo que llega `null` no se toca. **Ya no es «la cartera del tenant»** (P0, V87): esta petición mezcla dos autoridades y se comprueban **por separado**. Los hechos de la PROPIEDAD —descripción, ubicación, titulares, atributos y retiradas— los escribe **su `id_rol_responsable`**; los de cada ENCARGO —importe, exclusividad, vigencia y condiciones— **su propio `captacion.id_rol_agente`**, encargo por encargo. Ni un OR ni una herencia: responder por la propiedad no permite tocar el encargo ajeno, y tener un encargo no permite tocar la ficha. Una petición que sólo trae `operaciones`/`condiciones` **no** pide la autoridad de la propiedad, así que su agente la sigue operando aunque la propiedad esté FALTANTE. Cambiar el importe **añade** un hito; nunca sobrescribe el anterior. Desde F2.10 las dos autoridades se comprueban **bajo candado**: la fila de `propiedad` se toma al cargarla y las de los encargos que la petición toque también, así que una edición que llegue después de un traspaso o de una reasignación ya comiteados ve al nuevo responsable o agente y recibe el 403 de siempre en vez de aterrizar. {autoridad: AutoridadDePropiedad.exigirEdicion} | E4 |
| POST | `/propiedades/{id}/responsable` | BROKER, TENANT_ADMIN | **El traspaso de la autoridad de escritura** (P0-2, V87). Fija `propiedad.id_rol_responsable` y escribe su fila en `asignacion_responsable_propiedad` —propiedad, anterior (NULL si estaba FALTANTE), nuevo, quién autorizó, banda, motivo y fecha—, las dos cosas en la misma transacción. **Es la única forma de mover la autoridad después del alta** y la única de sacar a una propiedad de FALTANTE. El agente **no** puede: si el traspaso fuera del propio agente, la autoridad sería autoservicio. **No reasigna ningún encargo, no toca ningún atributo inmobiliario y no abre ningún histórico** — concede escritura sobre lo vigente, no lectura de lo pasado. **El alcance va después de la frontera de tenant y antes que nada más** (C1): un agente destino de otra corredora se comporta como **inexistente**; dentro del tenant, el **BROKER** sólo asigna a los agentes que **supervisa** (`alcances.alcanza`, el mismo que usa `POST /captaciones/{id}/reasignar`), y el **TENANT_ADMIN** a cualquier agente de su organización **aunque sea del equipo de otro broker** — está exento de la restricción de equipo, **nunca** de la de tenant. **Y un traspaso tiene DOS extremos, así que se comprueban los dos** (C6): con la propiedad **con** responsable, el BROKER necesita supervisar de forma vigente al **saliente** *y* al **destino** —si cualquiera de los dos es de otro equipo es un traspaso **entre** equipos y lo decide el TENANT_ADMIN—; con la propiedad **FALTANTE** no hay saliente a quien supervisar, así que **cualquier BROKER del tenant la gobierna para asignarla, pero sólo a uno de sus supervisados vigentes** (C5), y **asignada, la excepción se cierra sola y vuelve a mandar el EQUIPO**. Las dos preguntas salen del mismo `Alcances` que decide la lectura del expediente —`alcanza` para el destino, `alcanzaIncluidoSinDueno` para el saliente—: una segunda comparación de supervisión escrita aparte diverge, y diverge hacia el lado que concede de más. **Nota de cable** (C7, 2026-09-01): `responsabilidad.puedeTraspasar` de `GET /propiedades/{id}` responde **«el actor puede INICIAR ahora el cambio de responsable de ESTA propiedad, considerando su responsable actual»**, y para eso lleva las dos guardas que la ficha sí puede mirar: la **banda** (no ser AGENTE) y el alcance sobre el **SALIENTE** (`alcanzaIncluidoSinDueno` sobre el responsable vigente), que es el **mismo** predicado que pregunta este POST — la ficha no puede prometer lo que el POST va a negar. El **TENANT_ADMIN** sale `true` por **autoridad de gobierno del tenant**, no como super-broker: no gana edición de hechos ni autoridad comercial. Con la propiedad **FALTANTE** sale `true` para **cualquier BROKER del tenant**, porque no hay saliente a quien supervisar (C5 intacta). Lo que **no** sabe es el **destino** —en la ficha todavía no hay destino elegido— y **no autoriza nada**: el POST sigue siendo la autoridad final y vuelve a comprobar banda, saliente y destino. Motivo obligatorio y con el **mismo mínimo** que la reasignación de un encargo (`PoliticaComercial.exigirMotivoDeReasignacion`): el expediente es append-only y no se corrige después. Reasignar al mismo agente es 400. La fila viaja con `origen=TRASPASO`, que **no se deduce** del predecesor: la primera asignación de una propiedad FALTANTE tampoco lo tiene. **Y desde el 2026-09-01 el DESTINO tiene que poder RECIBIRLA** (D-P0-7): última guarda, después de todas las anteriores y antes de escribir nada, exigiendo las cinco condiciones del destino —mismo tenant, rol `AGENTE` vigente, cuenta habilitada, membresía vigente y disponibilidad operativa; más supervisión vigente si el actor es BROKER—, con **el mismo predicado SQL** que ofrece los candidatos de la fila de abajo. **Alcanzar a un agente no es que ese agente pueda operar**: son dos preguntas y las dos tienen que decir que sí, así que `alcances.alcanza` **no** se sustituye por esto. Va la última a propósito: adelantarla cambiaría el motivo del rechazo en casos que hoy fallan por otra razón, y un cambio de motivo es un cambio de contrato aunque el código HTTP coincida. El rechazo lleva **un solo mensaje** y no dice cuál de las cinco falló — publicar el estado administrativo de una cuenta ajena a quien sólo preguntaba por un traspaso sería información que no le corresponde. **Desactivar a un agente no reasigna nada** (D-P0-8): deja de poder **recibir**, y las propiedades que ya llevaba se quedan donde estaban, como situación que un traspaso explícito y trazable tiene que resolver. **Y desde el 2026-09-01 el comando declara DESDE DÓNDE se traspasa** (D-P0-9): el cuerpo lleva **exactamente una** de las dos declaraciones — `idResponsableActual`, el responsable que se vio en la ficha, o `sinResponsableActual: true`, que estaba **FALTANTE**—, y ninguna de las dos o las dos a la vez es **400**: un cuerpo sin declaración no dice «estaba FALTANTE» sino que **nadie miró**, y quedarse con la más probable sería inventar la observación. Si al ejecutarse el responsable ya **no** es el declarado, la respuesta es **409** y **no se ha escrito nada** — el traspaso **no se reinterpreta**: un «cambia A por C» que llega cuando ya responde B **no** se convierte en «cambia B por C», que es una decisión distinta de la que firmó la persona. Lo congelado es el comportamiento —de un estado concreto parte **exactamente una** transición legítima—; el mecanismo es una elección y hoy es precondición en memoria **más** compare-and-set en SQL (`UPDATE … WHERE id_rol_responsable = observado`), sin `@Version` y sin migración. **Y el traspaso es UN solo hecho** (D-P0-10): responsable, saliente, destino, actor, banda, motivo, fila del expediente y evento de dominio van en la **misma transacción**, y si cualquiera falla se deshacen todas — no queda responsable cambiado sin traza, ni traza de un cambio que no ocurrió, ni motivo de otra transición. Las dos cosas las comprueba `CausalidadDelTraspasoIntegrationTest` con **dos transacciones vivas a la vez** sobre PostgreSQL y con **fallos inyectados** en el rastro y en el evento, no leyendo el `@Transactional`. {autoridad: AutoridadDePropiedad.asignar} | E4 |
| GET | `/propiedades/{id}/responsable/historial` | BROKER, TENANT_ADMIN | **El expediente de traspasos es superficie de GOBIERNO** (C2), del más reciente al más antiguo. Lo lee el BROKER **dentro de su alcance de supervisión** —la propiedad tiene que responder ante uno de sus agentes, comprobado en el servicio con `alcances.alcanzaIncluidoSinDueno`— y el TENANT_ADMIN dentro de su tenant. **El AGENTE no, incluido el responsable vigente**: sabe que responde él y tiene lo que necesita para operar, pero **no hereda** responsables anteriores, motivos de traspaso ni observaciones de gobierno sobre agentes que ya no la llevan; el texto libre del motivo es dato interno de gobierno. Una propiedad **FALTANTE** la alcanza **cualquier BROKER de su tenant** (C5): gobernar el inventario sin dueño es trabajo de bróker —es lo que tiene que mirar para decidir a quién asignarlo—, y la regla de equipo existe para no cruzar equipos, así que sin responsable **no tiene sobre qué aplicarse**. Lo decide `Alcances.alcanzaIncluidoSinDueno`, no una rama en el llamador. Frontera de tenant **antes** que el rol: un id de otra corredora responde **404**, no 403. Si el nuevo responsable necesita contexto, la respuesta no es abrirle el expediente sino una **nota de traspaso operativa**, que no existe todavía. {autoridad: AutoridadDePropiedad.exigirLecturaDelExpediente} | E4 |
| GET | `/propiedades/{id}/responsable/candidatos` | BROKER, TENANT_ADMIN | **A quién puedo traspasarla: los candidatos YA elegibles para ESTE recurso y ESTE actor** (D-P0-7 + D-P0-12, 2026-09-01), paginado y buscable por nombre o código de agente. **Existe para que Angular no decida autoridad**: sin él, la pantalla del traspaso tendría que pedir la lista de agentes del tenant y depurarla con su propia copia de las cinco condiciones de elegibilidad —una lista de permisos viviendo en el cliente— o dejar que el bróker eligiera a ciegas y descubriera el rechazo después. Un candidato aparece si cumple **todas**: mismo tenant, rol `AGENTE` **vigente**, cuenta habilitada (`credencial_usuario.estado_administrativo='A'` de su rol `USUARIO_INTERNO` vigente), membresía vigente (`usuario_organizacion.estado='A'` por ese mismo rol), disponibilidad operativa (`detalle_agente.estado_operativo='D'`) y —sólo si el actor es **BROKER**— supervisión vigente sobre él; el **TENANT_ADMIN** no necesita supervisar, pero el destino tiene que ser igualmente un agente válido y operativo de su organización. **Supervisión vigente y agente operativo son invariantes distintas** y se comprueban por separado: un supervisado de baja no aparece, y un disponible de otro equipo tampoco. Se excluye al **responsable actual** —ofrecerlo sería ofrecer un traspaso que el POST rechaza con «ese agente ya responde por esta propiedad»—. **Alcance**: la frontera de tenant primero (un id ajeno responde **404**); después, el **mismo predicado que apaga el botón en la ficha** (`AutoridadDePropiedad.puedeIniciarTraspaso`: banda no-AGENTE **y** alcance sobre el saliente) — si el actor no puede iniciar el traspaso, responde **403** y no una lista vacía, porque «no hay candidatos» y «no te corresponde» son dos respuestas distintas. **Y no autoriza nada**: el `POST /propiedades/{id}/responsable` revalida banda, tenant, saliente, destino **y elegibilidad**, porque entre pedir la lista y usarla un agente puede quedar desactivado. La lista y esa revalidación comparten **el mismo predicado SQL** (`DetalleAgenteRepository.CONDICION_CANDIDATO`), justamente para que no puedan responder cosas distintas. No publica ningún estado administrativo: quien está, cumple; de quien no está, no se dice por qué. {autoridad: AutoridadDePropiedad.puedeIniciarTraspaso} | E4 |
| GET | `/encargos/{idEncargo}/publicaciones` | TODOS | **Los anuncios de UN encargo** (V70), del más reciente al más antiguo; un encargo de otro tenant responde **404**. Sustituye a `/locales/{id}/publicaciones`, que devolvía las series de venta y alquiler juntas sin poder decir cuál publicaba qué — porque la publicación no llevaba la operación. Cada anuncio viaja con su `idEncargo` y con `importeRotulo` («precio de venta» / «renta mensual»). | E4 |
| POST | `/encargos/{idEncargo}/publicaciones` | AGENTE | Publica el encargo en un canal, **y sólo su propio agente** (P0-4, V87): publicar escribe un hito `P` en la serie económica del encargo, así que hasta V87 cualquier agente del tenant metía una cifra en el histórico de otro. **Rechaza el encargo no vigente**: publicar uno cerrado pondría en el mercado algo que ya no se ofrece. El hito `P` nace atado a ESTE encargo y con SU operación, no suponiendo alquiler. Desde F2.10 la fila de `captacion` se **toma al cargarla**, así que el agente que se comprueba es el que seguirá siéndolo cuando el hito se escriba. {autoridad: AutoridadDePropiedad.exigirEdicionDelEncargo} | E4 |
| PUT | `/encargos/{idEncargo}/publicaciones/{idPublicacion}` | AGENTE | Edita un anuncio **de un encargo del actor** (P0-4, V87): llegar por el id de la publicación no puede ser una puerta más barata que llegar por el del encargo, porque las dos escriben la misma serie. Un anuncio sin encargo resuelto **se rechaza** en vez de suponer de quién es — y **desde `V89` (D-P0-11) ya no puede existir**: `publicacion.id_captacion` es NOT NULL, así que esa rama del servicio queda como **guarda defensiva**, no como caso vivo; sigue ahí porque también responde cuando el encargo es de **otro tenant** o el id es `<= 0`. Cambiar el importe **añade** un hito `P` si el anuncio está publicado; nunca sobrescribe el anterior. Desde F2.10 la fila de `captacion` del encargo se **toma al cargarla**, para que la autoridad decida sobre el agente que seguirá siéndolo al escribir. {autoridad: PublicacionServiceImpl.exigirEncargoPropio} | E4 |
| POST | `/encargos/{idEncargo}/publicaciones/{idPublicacion}/estado` | AGENTE | Publicar (`P`), pausar (`S`) o cerrar (`C`), **sólo el agente del encargo** (P0-4, V87). El instante en que un borrador pasa a publicado es la primera vez que ese importe lo ve el mercado, y por eso también escribe hito — la tercera puerta a la misma serie. Desde F2.10 la fila de `captacion` del encargo se **toma al cargarla**, igual que en las otras dos puertas. {autoridad: PublicacionServiceImpl.exigirEncargoPropio} | E4 |
| POST | `/captura` | TODOS | Abre o continúa un borrador del tenant. **No escribe nada del negocio**: sólo anota lo conocido y responde qué falta. | E4 |
| GET | `/captura` | TODOS | Los borradores en curso de la **organización**, no los de quien pregunta: es lo que permite que una captura iniciada por KAIROS la termine otra persona. | E4 |
| GET | `/captura/apertura` | TODOS | Qué hay que decidir **antes** de que exista un plan de preguntas: el tipo y la operación, con sus opciones y sus rótulos. El **tipo** es el que manda —decide qué se pregunta después—; la operación se ofrece pero **ya no bloquea** (V75): una propiedad puede registrarse para prospectarla y su encargo nace al captar. Sin alcance porque no devuelve datos de nadie. Existe para que ni Angular ni KAIROS escriban «primero el tipo, luego la operación»: lo dice el motor. | E4 |
| GET | `/captura/definicion` | TODOS | Qué campos aplican a un `tipoPropiedad` y, **opcionalmente**, a una o dos `operaciones`. Sin `operaciones` devuelve sólo la cosa física —comunes y sección del tipo, **cero bloques de encargo**—, que es la consulta de quien quiere saber qué tiene un departamento sin fingir todavía una intención comercial. Con `VENTA,ALQUILER` la ficha física viene una vez y la condición económica dos. **La operación no se infiere nunca**: la propiedad no tiene operación, la tiene el Encargo. Sin alcance porque no devuelve datos de nadie — devuelve la **definición**, que sale del catálogo del tenant (`catalogo_atributo`). Existe para que ni Angular ni KAIROS tengan su propia matriz «tipo → campos». | E4 |
| GET | `/captura/{id}` | TODOS | Un borrador del tenant. | E4 |
| POST | `/captura/{id}/ejecutar` | AGENTE | Corre el caso de uso con lo que el borrador sabe. Falla con la lista de lo que falta si no hay suficiente. {autoridad: AutoridadDePropiedad.fijarAlAlta} | E4 |
| DELETE | `/captura/{id}` | TODOS | Descarta el borrador del tenant. No lo borra: que alguien lo empezara también es un hecho. | E4 |
## F2 — Proceso (prospección y captación)

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/prospecciones` | TODOS | `Alcances`: AGENTE las suyas, BROKER las de sus supervisados vigentes, TENANT_ADMIN el tenant. | F2 |
| GET | `/prospecciones/{id}` | TODOS | Mismo alcance que el listado. | F2 |
| GET | `/prospecciones/recontactar` | TODOS | Recontacto vencido **dentro del alcance del actor**. | F2 |
| GET | `/prospecciones/{id}/coincidencias` | TODOS | Matching con vista personal (ver `/captaciones/{idOrCodigo}/coincidencias`). | F2 |
| POST | `/prospecciones` | AGENTE | Alta propia, estado inicial P. {autoridad: BANDA} | F2 |
| POST | `/prospecciones/{id}/contactar` | AGENTE | Solo sobre prospecciones suyas. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/reunion` | AGENTE | Solo sobre prospecciones suyas. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/propuesta` | AGENTE | Cable real: **la v1 nunca emite el estado `E`**; deja `S` y marca `fechaPropuesta` + `resultadoPropuesta='P'`. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/seguimiento` | AGENTE | Solo sobre prospecciones suyas. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/rechazar` | AGENTE | Exige observación (MEJ-03). {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/descartar` | AGENTE | Solo sobre prospecciones suyas. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/captar` | AGENTE | **Aquí nace el Encargo**: el propietario aceptó. Cuerpo obligatorio con `operacion` (VENTA o ALQUILER, **sin defecto**), `importe`, `moneda` y `comisionPactada`; el tipo y la base de comisión los implica la operación si no se declaran. Hasta V75 la operación estaba escrita a fuego como ALQUILER y el importe se copiaba del espejo de la propiedad. Rechaza abrir un segundo encargo **vivo** de la misma operación, y lo dice con palabras; el de la otra operación sí se puede. Emite `CAPTACION_CREADA` (corregido el 2026-08-08). {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| POST | `/prospecciones/{id}/marcar-captado` | AGENTE | Enlaza una captación ya creada por el agente. {autoridad: ProspeccionServiceImpl.cargarEnProceso} | F2 |
| GET | `/captaciones` | TODOS | `Alcances`: AGENTE las suyas, BROKER las de sus supervisados, ADMIN el tenant. | F2 |
| GET | `/captaciones/{id}` | TODOS | Mismo alcance que el listado; fuera de él, 403. **Publica además `capacidades` resueltas por el Core** (D-P0-12, 2026-09-01): `puedeEditar`, `puedeRevisar`, `puedeCerrar` y **`puedeReasignar`**, cada una calculada con **el mismo predicado que después deniega el comando** —no con una segunda tabla de decisión—, de modo que la pantalla no pueda ofrecer un botón que el backend va a rechazar. Angular no decide autoridad: sin esto, la ficha del encargo tendría que reescribir esas reglas («soy el agente y está pendiente», «soy bróker y lo superviso», «está activa») y una copia de una regla de autoridad diverge siempre hacia el lado que concede de más. El **TENANT_ADMIN** recibe `puedeRevisar=false` y `puedeCerrar=false` aunque alcance el encargo para leerlo — y **`puedeReasignar=true`**, que es la única de las cuatro que el gobierno hereda porque reasignar entre equipos es organigrama y no operación comercial (D-S0-17 fila 6). `puedeReasignar` son las guardas del comando **sin el destino**, que en la ficha todavía no existe —bróker que supervisa hoy al agente que lo lleva, o gobierno del tenant—, y es **el mismo predicado** que decide el 403 de `/captaciones/{id}/reasignacion/candidatos`. Un AGENTE recibe `false` incluso sobre el encargo que lleva: quien lleva un encargo no decide dejar de llevarlo. **No autoriza nada**: el comando revalida. Sólo viaja en las fichas **individuales**; en los listados llega nulo y con `NON_NULL` no viaja, y su ausencia significa «no calculado aquí», no «no puedes». | F2 |
| GET | `/captaciones/codigo/{codigo}` | TODOS | Mismo alcance que `{id}`, y **la misma forma**: es la segunda puerta al mismo recurso, así que sale del mismo productor de ficha individual y publica también `capacidades`. Una puerta que las publicara y otra que no obligaría al SPA a llevar la regla en la que se olvida. | F2 |
| GET | `/captaciones/pendientes` | BROKER, TENANT_ADMIN | **Supervisión.** Bandeja de revisión: las P u O del equipo del broker; el TENANT_ADMIN, las del tenant **y solo para verlas** — decidirlas es la fila siguiente, que ya no alcanza. Ver la cola no produce ningún hecho. {autoridad: Alcances.de} | F2 |
| GET | `/captaciones/reasignables` | BROKER, TENANT_ADMIN | **Supervisión.** Las ACTIVAS dentro del alcance. Es el insumo de la reasignación, que el TENANT_ADMIN sí conserva. {autoridad: Alcances.de} | F2 |
| GET | `/captaciones/propiedades-equipo` | BROKER, TENANT_ADMIN | **Extensión aditiva** (no existe en la v1). Mismo alcance que el listado —BROKER sus supervisados, ADMIN el tenant— pero **una fila por INMUEBLE**: deduplica por propiedad quedándose con la captación más reciente. Filtra (`texto`, `distrito`), ordena, pagina y cuenta **en SQL**. {autoridad: Alcances.de} | F2 |
| GET | `/captaciones/propiedades-equipo/resumen` | BROKER, TENANT_ADMIN | Mismo alcance y mismo `texto` que la lista. Cuenta **inmuebles distintos** en la base, no filas descargadas, y devuelve los distritos disponibles para el filtro. No acepta `distrito`: el resumen es lo que ese filtro acota. {autoridad: Alcances.de} | F2 |
| GET | `/captaciones/{idOrCodigo}/coincidencias` | TODOS | **Vista personal**: para un actor no-ADMIN la demanda propia son los clientes que YA tienen oportunidad del equipo, así que un cliente recién creado no aparece aunque case al 100 %. | F2 |
| POST | `/captaciones` | AGENTE | Alta propia sobre un local sin captación ACTIVA. {autoridad: BANDA} | F2 |
| PUT | `/captaciones/{id}` | AGENTE | Solo las suyas; editar una OBSERVADA la reenvía a PENDIENTE. Desde F2.10 la fila de `captacion` se **toma al cargarla** y la autoridad se comprueba bajo ese candado, así que una edición que llegue después de una reasignación ya comiteada ve al nuevo agente y recibe el 403 de siempre en vez de reescribir el importe, la exclusividad y la vigencia de un encargo que ya lleva otro. {autoridad: AutoridadDePropiedad.exigirEdicionDelEncargo} | F2 |
| POST | `/captaciones/{id}/decision` | BROKER | **Operación comercial — el TENANT_ADMIN dejó de alcanzarla (D-S0-17 fila 5).** Es el juicio profesional sobre un encargo: quién entra a cartera lo decide el broker, no quien administra cuentas. Aprobar/observar/rechazar dentro de su alcance; observar y rechazar exigen observación. **Y la banda se exige también en el Core** (2026-09-01): hasta aquí lo único que frenaba al TENANT_ADMIN era el `@PreAuthorize` del controlador, y una anotación protege **una puerta** mientras la regla tiene que proteger **el hecho** — KAIROS entra por este mismo caso de uso, y `cargarConAcceso` le dice que sí porque el gobierno **sí** alcanza el encargo para leerlo. La guarda va **después** de esa carga para conservar el **404** de otro tenant: un id ajeno no puede empezar a responder 403. {autoridad: CaptacionServiceImpl.exigirBandaComercial} | F2 |
| POST | `/captaciones/{id}/reasignar` | BROKER, TENANT_ADMIN | **Las dos cosas a la vez (fila 6).** Reasignar **dentro** del equipo es supervisión y la conserva el BROKER; reasignar **entre** equipos es organigrama, y eso es gobierno del TENANT_ADMIN. Mismo endpoint, **dos alcances distintos**. Evento de actor (tabla-evento), no transición. **Quién puede no cambió el 2026-09-01, y lo demás sí** (F2 · ENCARGO): (a) **es la ÚNICA puerta canónica** que mueve `captacion.id_rol_agente` — la asociación está mapeada `updatable = false`, así que ningún *flush* de entidad gestionada, `PUT /captaciones/{id}`, mapper, copia ni cascada puede moverla, y en `UPDATE` sólo la escribe `CaptacionRepository.cambiarAgenteSi`; (b) **el destino tiene que poder RECIBIRLO hoy** (D-P0-7), con las **cinco condiciones** y **el mismo predicado SQL** que el traspaso de una propiedad — alcanzar a un agente y que ese agente pueda operar son dos preguntas, y las dos tienen que decir que sí; (c) **la frontera de tenant va antes del alcance**: `findById` no filtraba por organización, así que un rol de otra corredora llegaba a la comprobación de supervisión; ahora responde «Agente no encontrado.», lo mismo que si no existiera en ninguna parte; (d) **el cuerpo declara DESDE DÓNDE**: `idAgenteActual` —el agente que se vio en la lista— es **obligatorio** y su ausencia es **400** («un cambio de autoridad no parte de un estado que nadie miró»); no hay equivalente a `sinResponsableActual` porque `id_rol_agente` es NOT NULL desde V5 y FALTANTE no es un estado observable aquí; (e) si al ejecutarse el encargo ya **no** lo lleva ese agente, la respuesta es **409** y **no se ha escrito nada** — la reasignación **no se reinterpreta**: un «cambia A por C» que llega cuando ya lo lleva B **no** se convierte en «cambia B por C». Lo congelado es el comportamiento —de un agente concreto parte **exactamente una** reasignación legítima—; el mecanismo es precondición en memoria **más** compare-and-set nativo (`update captacion set id_rol_agente = :nuevo where … and id_rol_agente = :esperado`), sin `@Version` y sin migración; (f) **y es UN solo hecho**: columna y fila de `reasignacion_captacion` van en la misma transacción y si cualquiera falla se deshacen las dos. Lo mide `CausalidadDeLaReasignacionIntegrationTest` con **dos transacciones vivas a la vez** sobre PostgreSQL, **fallo inyectado** en el rastro y el ataque de **dirty checking** de una edición concurrente. **Desactivar a un agente no reasigna sus encargos** (D-P0-8): siguen siendo suyos, y sacarlos es esta misma decisión explícita. {autoridad: Alcances.alcanza} | F2 |
| GET | `/captaciones/{id}/reasignacion/candidatos` | BROKER, TENANT_ADMIN | **A quién puedo pasarle este encargo** (D-P0-7 + D-P0-12, 2026-09-01). Los destinos **ya elegibles** para ESTE encargo y ESTE actor, paginados y buscables **en el servidor**. **Existe para que Angular no decida autoridad**: hasta aquí la pantalla de reasignaciones pedía `GET /agentes?pagina=1&tamano=100` y depuraba en el cliente con **dos** de las seis condiciones (cuenta activa y disponibilidad) sobre **una página** — una copia parcial de una regla de autoridad, que ofrecía agentes que el POST rechaza y escondía a otros válidos en cuanto la organización pasara de cien. Sale del **mismo componente** y del **mismo predicado SQL** (`DetalleAgenteRepository.CONDICION_CANDIDATO`) que los candidatos de una propiedad: quién puede recibir una propiedad y quién puede recibir un encargo responden a las mismas condiciones, y lo único que cambia es **a quién se excluye** —allí el responsable actual, aquí el agente actual del encargo—. El alcance lo decide el servicio: un id de otra corredora es **404** (`cargarConAcceso`), y un actor que no puede reasignar este encargo —**el mismo predicado que apaga `capacidades.puedeReasignar`**— es **403**, no una lista vacía: «no hay candidatos» y «no te corresponde» son dos respuestas distintas. **No autoriza nada**: el POST revalida banda, tenant, destino, elegibilidad y agente observado. {autoridad: CaptacionServiceImpl.puedeReasignar} | F2 |
| POST | `/captaciones/{id}/cierre` | BROKER | **Operación comercial — el TENANT_ADMIN dejó de alcanzarla (fila 7).** Cerrar un encargo tiene efecto sobre disponibilidad y cartera. **La banda se exige también en el Core** (2026-09-01), por la misma razón y con el mismo orden que la decisión: después de `cargarConAcceso`, para que otro tenant siga siendo 404. El cierre **por contrato** (`cerrarPorContrato`, cascada de `POST /contratos`) **no** lleva esta guarda a propósito: es el efecto de una firma, no una decisión de banda, y exigirla ahí dejaría contratos que no se pueden cerrar. {autoridad: CaptacionServiceImpl.exigirBandaComercial} | F2 |
| GET | `/captaciones/reasignaciones` | BROKER, TENANT_ADMIN | **Rastro de gobierno del ENCARGO**, el más reciente primero: de qué agente salió, hacia cuál fue, quién lo firmó y **con qué motivo**. **Se acota con el MISMO alcance que el listado de captaciones** (`Alcances.de` sobre `captacion.id_rol_agente`): el **TENANT_ADMIN** ve todo su tenant, y el **BRÓKER** las filas de los encargos que **hoy** lleva un agente al que supervisa. Es la aplicación de D-P0-6 —«BROKER, encargos dentro de su alcance; TENANT_ADMIN, todo el tenant, por gobierno del expediente de traspasos»— a esta superficie, sin regla nueva: **quien ve el encargo ve su rastro**. **El alcance es el del encargo de HOY, y eso es interpretación declarada** (2026-09-02): no el del agente **saliente** —un encargo que ya no lleva su equipo seguiría en el rastro del bróker para siempre— ni el del **bróker que firmó** —una reasignación de gobierno no la vería ningún bróker—. **La banda va delante y no la sustituye el alcance**: a un AGENTE `Alcances.de` le devolvería «lo suyo», que aquí es justo lo que no le toca, así que el Core repite la anotación con `exigirBandaQueGobiernaElEncargo` — de otro modo KAIROS y cualquier consumidor que no pase por HTTP entrarían por debajo del `@PreAuthorize`. Hasta el 2026-09-02 esta fila confesaba autoridad `TENANT` y decía la verdad: el servicio consultaba `findByOrganizacionIdOrderByIdDesc` sin pasar por `Alcances`, y un bróker leía los motivos de equipos que no supervisa. **El derivado ya no existe** — mientras quedara, el siguiente consumidor podía volver a tomarlo. Ni el DTO ni el orden cambian. {autoridad: Alcances.de} | F2 |

## F3 — Demanda

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/clientes` | TODOS | **Catálogo compartido**: ADMIN y AGENTE ven y editan todos los del tenant, sin regla de pertenencia. El único acotado es el BROKER, y su conjunto se deriva de **las oportunidades de su equipo**. | F3 |
| GET | `/clientes/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismos filtros que el listado, salvo `estado`: es uno de los cubos que devuelve. Cuenta en la base sobre el mismo conjunto de candidatos que pagina la lista, y devuelve los rubros disponibles para el selector. | F3 |
| GET | `/clientes/{id}` | TODOS | Mismo catálogo compartido. | F3 |
| GET | `/clientes/{id}/coincidencias` | TODOS | Matching con vista personal. | F3 |
| GET | `/clientes/{id}/autorizacion` | TODOS | **Aditivo (D-27), no existe en la v1.** Constancia de la autorización de datos para la ficha: estado, cuándo y quién la registró. Mismo alcance que `GET /clientes/{id}` — se resuelve con el mismo `cargarConAcceso`, así que un cliente fuera del alcance del BROKER responde 403 y uno de otro tenant 404. La consulta va por `persona.id`, no por el id del rol: la autorización la dio la persona una vez y cubre todos sus roles. | F3 |
| POST | `/clientes` | AGENTE | Alta en el tenant del actor. {autoridad: TENANT} | F3 |
| PUT | `/clientes/{id}` | AGENTE | Catálogo compartido: el agente edita cualquiera de su organización. {autoridad: ClienteServiceImpl.cargarConAcceso} | F3 |
| DELETE | `/clientes/{id}` | AGENTE | Baja lógica. {autoridad: TENANT} | F3 |
| GET | `/requerimientos/cliente/{idCliente}` | TODOS | Por tenant, colgado del cliente. | F3 |
| POST | `/requerimientos` | AGENTE | Sobre un cliente del tenant. {autoridad: TENANT} | F3 |
| PUT | `/requerimientos/{id}` | AGENTE | Sobre un requerimiento del tenant. {autoridad: TENANT} | F3 |
| POST | `/requerimientos/{id}/estado` | AGENTE | Cambio de estado del requerimiento. {autoridad: TENANT} | F3 |
| GET | `/oportunidades` | TODOS | AGENTE las suyas; **BROKER las de SUS CAPTACIONES**, no las de sus agentes; ADMIN el tenant. Distinto de interacciones **a propósito**. | F3 |
| GET | `/oportunidades/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismos filtros que el listado, salvo `estado`: son los cubos que devuelve. Cuenta por etapa **en la base**, con un solo `group by` sobre el mismo conjunto que pagina la lista. | F3 |
| GET | `/oportunidades/{id}` | TODOS | Mismo alcance que el listado. | F3 |
| POST | `/oportunidades` | AGENTE | La captación indicada debe ser del agente que registra, o 403. {autoridad: OportunidadServiceImpl.registrar} | F3 |
| POST | `/oportunidades/{id}/no-continuidad` | AGENTE | Cierre A→N con motivo tipificado. {autoridad: OportunidadServiceImpl.cargarConAcceso} | F3 |
| POST | `/oportunidades/{id}/cierre-exitoso` | AGENTE | **Responde 400 siempre** y es correcto: el cierre exitoso lo produce la cascada del contrato (F4), no un botón. {autoridad: OportunidadServiceImpl.cargarConAcceso} | F3 |
| GET | `/visitas` | TODOS | AGENTE las suyas; BROKER **las de sus captaciones**; ADMIN todas las del tenant. | F3 |
| GET | `/visitas/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismo `query` que el listado. Cuenta los cinco estados **en la base** y devuelve los distritos disponibles para el filtro. No acepta `estado` ni `distrito`: son los filtros que acota. | F3 |
| GET | `/visitas/{id}` | TODOS | Mismo alcance que el listado. | F3 |
| GET | `/visitas/proximas` | TODOS | Mismo alcance, ventana futura. | F3 |
| GET | `/visitas/mes` | TODOS | Mismo alcance, agenda del mes. | F3 |
| POST | `/visitas` | AGENTE | Ojo: exige que la oportunidad sea del **propio** agente, **sin** alcance de broker. {autoridad: TENANT} | F3 |
| PATCH | `/visitas/{id}/reprogramar` | AGENTE | Sobre visitas suyas; queda auditada. {autoridad: VisitaServiceImpl.cargarConAcceso} | F3 |
| PATCH | `/visitas/{id}/cancelar` | AGENTE | Sobre visitas suyas. {autoridad: VisitaServiceImpl.cargarConAcceso} | F3 |
| PATCH | `/visitas/{id}/realizar` | AGENTE | Sobre visitas suyas. {autoridad: VisitaServiceImpl.cargarConAcceso} | F3 |
| PATCH | `/visitas/{id}/no-realizada` | AGENTE | Sobre visitas suyas. {autoridad: VisitaServiceImpl.cargarConAcceso} | F3 |
| PATCH | `/visitas/{id}/resultado` | AGENTE | Resultado tipificado de la visita. {autoridad: VisitaServiceImpl.cargarConAcceso} | F3 |
| GET | `/interacciones` | TODOS | AGENTE las suyas; **BROKER por AGENTE SUPERVISADO**; ADMIN todo. Distinto de oportunidades/visitas, que alcanzan por captación: **no unificar**. | F3 |
| GET | `/interacciones/{id}` | TODOS | Mismo alcance que el listado. | F3 |
| POST | `/interacciones` | AGENTE | Polimórfica: cuelga de una de cuatro entidades según `contexto`, garantizado por CHECK en la BD. {autoridad: TENANT} | F3 |
| PUT | `/interacciones/{id}` | AGENTE | Sobre interacciones suyas. {autoridad: InteraccionServiceImpl.cargarConAcceso} | F3 |

## E3 — Ficha comercial (lectura transversal)

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/clientes/{id}/ficha-comercial` | TODOS | AGENTE ve **solo su historia y sin nombre de agente**; BROKER su equipo o las captaciones que revisa; ADMIN el tenant. Carga inicial parcial, tope de 8 filas. | E3 |
| GET | `/clientes/{id}/ficha-comercial/{section}` | TODOS | Igual que la ficha completa; aliases `page`/`pagina` y `page_size`/`tamano`. | E3 |
| GET | `/propietarios/{id}/ficha-comercial` | TODOS | Mismo criterio que la de cliente. La cabecera responde `cantidadLocales=0` por paridad con el cable. | E3 |
| GET | `/propietarios/{id}/ficha-comercial/{section}` | TODOS | Igual que la ficha completa. | E3 |

## F4 — Solicitud → contrato

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/solicitudes` | TODOS | AGENTE las suyas; **BROKER por AGENTE SUPERVISADO**; ADMIN el tenant. Ojo: contratos alcanzan por captación — son dos reglas distintas y no se unifican. Los filtros `idAgente`, `estado`, `distrito` y `texto` son **aditivos**: omitidos, la respuesta es la del cable v1. `estado=PENDIENTES` no es un estado, es el cubo `{E,O}` de la cola del broker. | F4 |
| GET | `/solicitudes/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismo `texto` que el listado. Cuenta los siete estados **en la base**, con un solo `group by` sobre el mismo conjunto que pagina la lista, y devuelve los distritos y agentes disponibles para los filtros. No acepta `estado`, `distrito` ni `idAgente`: son justo lo que acota. | F4 |
| GET | `/solicitudes/{id}` | TODOS | Mismo alcance que el listado. | F4 |
| GET | `/solicitudes/codigo/{codigo}` | TODOS | Mismo alcance que `{id}`. | F4 |
| POST | `/solicitudes` | AGENTE | Exige captación ACTIVA y oportunidad ABIERTA; el alta **no** comprueba que la oportunidad sea del agente (cable real). {autoridad: SolicitudServiceImpl.exigirAgenteDisponible} | F4 |
| POST | `/solicitudes/{id}/reenviar` | AGENTE | Solo desde REGISTRADA u OBSERVADA, y exige que el agente tenga broker supervisor activo. {autoridad: AccesoSolicitud.conAcceso} | F4 |
| GET | `/solicitudes/{id}/evaluaciones` | TODOS | Historial de la solicitud: lo ve **también el agente dueño**, a diferencia de `/evaluaciones`. | F4 |
| GET | `/solicitudes/{id}/documentos` | TODOS | Por agente, igual que la solicitud. | F4 |
| POST | `/solicitudes/{id}/documentos/archivo` | AGENTE | Subida octet-stream. {autoridad: AccesoSolicitud.conAcceso} | F4 |
| PATCH | `/solicitudes/{id}/documentos/{idDoc}/revisar` | BROKER | **Operación comercial — el TENANT_ADMIN dejó de alcanzarla (fila 9).** Juicio sobre un expediente. **D-F4-5 cerrada**: comprueba el alcance del broker y responde **403** donde la v1 respondía 200. Divergencia deliberada. {autoridad: AccesoSolicitud.conAcceso} | F4 |
| PATCH | `/solicitudes/{id}/documentos/conformar` | BROKER | **Operación comercial — el TENANT_ADMIN dejó de alcanzarla (fila 10).** Conformidad en bloque, con alcance de broker; separarla de la anterior abriría el hueco que D-F4-5 cerró. {autoridad: AccesoSolicitud.conAcceso} | F4 |
| GET | `/evaluaciones` | BROKER, TENANT_ADMIN | **Supervisión.** El broker ve **solo las que él firmó**; el TENANT_ADMIN, las del tenant: auditar qué se aprobó es justo lo que un administrador debe poder hacer, aunque ya no pueda firmarlo. El agente no entra aquí (sí a `/solicitudes/{id}/evaluaciones`). {autoridad: EvaluacionServiceImpl.listar} | F4 |
| GET | `/evaluaciones/{id}` | BROKER, TENANT_ADMIN | Mismo alcance que el listado. {autoridad: EvaluacionServiceImpl.obtener} | F4 |
| POST | `/evaluaciones` | BROKER | **La más sensible de las 18, y el TENANT_ADMIN dejó de alcanzarla (fila 13).** Es la decisión que desemboca en contrato y comisión: firmarla es responsabilidad profesional del broker. El tipo se **deriva del resultado** (el broker no lo elige), pero el request exige `tipoEvaluacion` presente y válido aunque luego lo pise. {autoridad: EvaluacionServiceImpl.exigirSupervisionSobreElAgente} | F4 |
| GET | `/contratos` | TODOS | AGENTE los suyos vía el agente de la solicitud; **BROKER por CAPTACION supervisada**; ADMIN el tenant. `montoAgente`/`montoEmpresa` solo viajan para ADMIN/BROKER: el agente no ve el reparto. Tamaño por defecto **100**, no 10. Acepta filtros **aditivos** (`texto`, `distrito`, `idAgente`) y `orden=cierre`; omitidos, responde igual que antes —incluido el orden congelado por id descendente—. El filtro por agente usa el de la **solicitud**, que es el que la respuesta publica. | F4 |
| GET | `/contratos/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismo `texto` que el listado. Suma la comisión y cuenta los cierres **en la base**, no sobre las filas descargadas: con el tope de 100 por página, un total calculado en el cliente sería falso pasados los 100 cierres. Devuelve además los distritos y agentes disponibles para los filtros. No acepta `distrito` ni `idAgente`: son los filtros que acota. | F4 |
| GET | `/contratos/oportunidad/{idOportunidad}` | TODOS | Mismo alcance que el listado. | F4 |
| POST | `/contratos` | AGENTE | Sobre una solicitud APROBADA suya; dispara la cascada de siete efectos que cierra el ciclo. {autoridad: TENANT} | F4 |
| POST | `/contratos/en-proceso` | AGENTE | Crea el borrador contractual P sobre una solicitud APROBADA propia, sin cerrar todavía la operación. {autoridad: TENANT} | F4 |
| POST | `/contratos/{idContrato}/firmar` | AGENTE | P → D; completa el snapshot y ejecuta el cierre comercial auditado. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/activar` | AGENTE | D → V con fecha efectiva, actor, rol y motivo. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/finalizar` | AGENTE | V → F y crea una tarea de revisión del inmueble sin reactivarlo. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/rescindir` | BROKER | **BROKER sin ADMIN** *(cambiado 2026-08-06, Bloque 7)*: «el broker decide, el agente registra». Rescindir corta un alquiler **que ya producía efectos** y arrastra consecuencias económicas; no es el registro de un hecho consumado como finalizar por plazo. Un `TENANT_ADMIN` sin rol operativo de broker recibe **403**: administrar el tenant no produce decisiones comerciales (D-S0-17). V → S y crea una tarea de revisión del inmueble **sin reactivarlo**. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/anular` | BROKER | **BROKER sin ADMIN** *(cambiado 2026-08-06, Bloque 7)*, por lo mismo que rescindir: anular deja sin efecto un contrato. Solo P/D → A — **anular un contrato VIGENTE se rechaza**, y esa es la diferencia con rescindir: borraría de la historia un alquiler que existió y por el que se cobró comisión. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/renovar` | AGENTE | V → R y crea un contrato sucesor enlazado, preservando el snapshot anterior. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/comision/asignar` | BROKER | **BROKER sin ADMIN**, a propósito: funciona porque el filtro JWT publica una sola authority `ROLE_<rol>`. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/comision/cobro` | BROKER | **BROKER sin ADMIN**, igual que asignar. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/comision/movimientos` | BROKER | Registra evidencia económica de cobro, pago al agente o reversión, en la moneda de la liquidación. **El ajuste (`A`) ya no se acepta**: nunca tuvo regla económica. Acepta `Idempotency-Key`. {autoridad: ContratoServiceImpl.cargarConAcceso} | F4 |
| POST | `/contratos/{idContrato}/revision-disponibilidad` | BROKER | Recupera la disponibilidad de un inmueble tras terminar el contrato: `VOLVER_AL_MERCADO` (`A→D`) o `RETIRAR_DEL_MERCADO` (`A→T`), con motivo obligatorio. Alcance por CAPTACIÓN, igual que el resto del recurso. Mismo gate que rescindir: es un hecho comercial sobre el inmueble. {autoridad: ContratoServiceImpl.exigirAlcance} | 7.3.2 |

## F6 / F7 — Alertas y bandeja

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/alertas` | TODOS | AGENTE las suyas; BROKER las de sus supervisados; ADMIN el tenant. **No hay columna de destinatario**: la alerta cuelga siempre de un AGENTE y quién la lee lo decide el TIPO. El GET **escribe**: materializa el barrido de recontacto como mucho una vez cada 5 min. | F6 |
| POST | `/alertas/{id}/atender` | TODOS | Sobre alertas en su alcance; la segunda llamada responde `false` sin ser error. | F6 |
| PATCH | `/alertas/{id}/atender` | TODOS | Mismo comportamiento que el POST (los dos verbos son del cable). | F6 |
| GET | `/tareas` | AGENTE | Bandeja **estrictamente personal**. Es el **único recurso del sistema sin acceso de ADMIN**. El GET **escribe**: deriva y reconcilia antes de devolver. Corta en 10 y descarta el resto en silencio. {autoridad: TareaServiceImpl.bandejaDe} | F7 |
| GET | `/tareas/pendientes` | AGENTE | Igual de personal que `/tareas`. {autoridad: TareaServiceImpl.bandejaDe} | F7 |
| POST | `/tareas/{id}/cancelar` | AGENTE | Cancelar **la mata para siempre**: el reconcile no la vuelve a crear aunque el disparador siga vigente. No es "más tarde". {autoridad: TareaServiceImpl.cancelar} | F7 |

## E1 — Personas y perfil

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/propietarios` | TODOS | ADMIN y AGENTE ven el catálogo entero del tenant; el **BROKER** se acota por sus **propiedades** (vía captación o prospección), no por oportunidades. `cantidadLocales` es un contador **con alcance**: dos actores ven números distintos del mismo propietario, y es correcto. Los filtros `texto` y `estado` son **aditivos**: omitidos, la respuesta es la del cable v1, orden por id descendente incluido. | E1 |
| GET | `/propietarios/resumen` | TODOS | **Extensión aditiva** (no existe en la v1). Mismo alcance y mismo `texto` que el listado; cuenta en la base sobre el MISMO conjunto que pagina la lista. No acepta `estado`: es el cubo que devuelve. | E1 |
| GET | `/propietarios/{id}` | TODOS | Mismo alcance que el listado. | E1 |
| GET | `/propietarios/{id}/autorizacion` | TODOS | **Aditivo (D-27), no existe en la v1.** Misma constancia y misma forma que la de cliente: es el mismo hecho sobre la misma persona. Alcance idéntico al de `GET /propietarios/{id}`, resuelto por el mismo `cargarConAcceso`. | E1 |
| POST | `/propietarios` | AGENTE | Responde `cantidadLocales` en 0 por paridad con el cable. {autoridad: TENANT} | E1 |
| PUT | `/propietarios/{id}` | AGENTE | También responde el contador en 0: la v1 no lo recalcula. {autoridad: PropietarioServiceImpl.cargarConAcceso} | E1 |
| DELETE | `/propietarios/{id}` | AGENTE | Baja lógica: la persona queda en estado I. {autoridad: TENANT} | E1 |
| GET | `/agentes` | BROKER, TENANT_ADMIN | Gate declarado **a nivel de clase**. TENANT_ADMIN el tenant (es su padrón); **BROKER solo los agentes que supervisa**. Los filtros `texto`, `estado`, `estadoOperativo` y `zona` son **aditivos**: omitidos, la respuesta es la del cable v1. El estado administrativo se filtra por la **credencial**, no por el agente. {autoridad: AgenteServiceImpl.exigirBrokerOAdmin} | E1 |
| GET | `/agentes/resumen` | BROKER, TENANT_ADMIN | **Extensión aditiva**. Mismo alcance y mismos filtros que el listado, menos `zona`: es una de las opciones que devuelve. Dos cubos por dos máquinas distintas —administrativo (credencial) y operativo (agente)—, más las zonas del alcance completo para el selector. {autoridad: AgenteServiceImpl.exigirBrokerOAdmin} | E1 |
| GET | `/agentes/{id}` | BROKER, TENANT_ADMIN | **Extensión aditiva** (la v1 no tenía GET individual). Ficha completa: identidad, supervisión vigente, captaciones/oportunidades/solicitudes por estado, cierres y las cuatro magnitudes de comisión por moneda. **El BROKER solo alcanza a los agentes que supervisa hoy**; fuera de su equipo responde 403. Los cierres y el dinero salen de la **atribución histórica de V27** (`contrato_alquiler.id_rol_agente_cierre`), no de la cadena solicitud→agente: un agente que cambió de equipo conserva su historia. {autoridad: AgenteServiceImpl.exigirBrokerOAdmin} | E1 |
| POST | `/agentes` | TENANT_ADMIN | **Gobierno del tenant — el BROKER dejó de alcanzarla (fila 17, D-S0-18: un broker no crea cuentas).** Exige `idBrokerSupervisor` **nuevo y obligatorio**: antes el supervisor era el broker de la sesión, y quien gobierna no supervisa a nadie de quien deducirlo. Crea atómicamente persona + `USUARIO_INTERNO` + rol + credencial + **membresía** + supervisión inicial. Responde contadores comerciales en 0 (rareza del cable). {autoridad: AgenteServiceImpl.exigirGobierno} | E1 |
| PUT | `/agentes/{id}` | TENANT_ADMIN | **Gobierno — el BROKER dejó de alcanzarla (fila 18).** Lo editable es identidad administrativa: el `PUT` ya descartaba en silencio documento, usuario, contraseña y código. También responde contadores en 0. **Y desde el 2026-09-01 toma el bloqueo de gobierno del agente** (D-P0-13) cuando el cuerpo trae `estado` o `estadoOperativo`: son los dos campos que cambian su **elegibilidad** (D-P0-7), y sin serializar contra la fila `detalle_agente` una baja podía colarse **entre** el «este destino vale» de un traspaso y su escritura — dejando la propiedad, o el encargo, en manos de alguien que ya no podía recibirlo con todas las guardas verdes. El candado no decide el orden, decide que **haya** uno: si la baja entra primero el traspaso se rechaza; si entra después queda un agente desactivado que lleva cartera, que es la situación legítima de D-P0-8. **No** se toma cuando el cuerpo sólo cambia teléfono, correo o zona: eso no mueve la elegibilidad y bloquear siempre convertiría una edición inocua en un punto de espera. {autoridad: AgenteServiceImpl.exigirGobierno} | E1 |
| GET | `/brokers` | TODOS | Por **tenant**, sin filtro de rol: los tres roles ven el mismo catálogo de brokers de su organización. | E1 |
| GET | `/brokers/{id}` | TODOS | Por tenant. | E1 |
| GET | `/brokers/{id}/agentes` | TODOS | Equipo vigente del broker. Conserva la rareza del cable: contadores comerciales en 0. | E1 |
| POST | `/brokers` | TENANT_ADMIN | Alta atómica persona + usuario interno + rol + credencial + membresía. Con `esAdministrador`, además concede gobierno (membresía `TENANT_ADMIN` + rol `ADMIN`): **ya no hay límite de administradores por organización**; lo único que sigue siendo único es el booleano heredado que lee GlassFish. {autoridad: BrokerServiceImpl.validarAdministrador} | E1 |
| PUT | `/brokers/{id}` | TENANT_ADMIN | Solo el TENANT_ADMIN edita brokers. {autoridad: BrokerServiceImpl.validarAdministrador} | E1 |
| GET | `/asignaciones/agentes` | TENANT_ADMIN | Gate declarado **a nivel de clase**. Gobierno: tenant completo. {autoridad: AsignacionServiceImpl.validarAdministrador} | E1 |
| GET | `/asignaciones/brokers` | TENANT_ADMIN | Gobierno: tenant completo. {autoridad: AsignacionServiceImpl.validarAdministrador} | E1 |
| GET | `/asignaciones/historial` | TENANT_ADMIN | Evento histórico de reasignación agente→broker (V10). {autoridad: AsignacionServiceImpl.validarAdministrador} | E1 |
| POST | `/asignaciones/reasignar` | TENANT_ADMIN | Cierra la supervisión anterior y abre la nueva en una sola transacción, respetando el único parcial. **Toma el bloqueo de gobierno del agente antes de tocar las supervisiones** (D-P0-13, 2026-09-01): cambiar de supervisor cambia la **sexta** condición de D-P0-7 —«¿lo supervisas hoy?»— para cualquier BROKER, así que esta operación se serializa contra la misma fila `detalle_agente` que bloquea `exigirElegible`. Va **antes** de comprobar los estados del agente para que lo que se lea aquí siga siendo verdad al escribir. {autoridad: AsignacionServiceImpl.validarAdministrador} | E1 |
| GET | `/perfil` | TODOS | El actor **sobre sí mismo**; no hay alcance que resolver. | E1 |
| PATCH | `/perfil` | TODOS | Solo teléfono: **no hay endpoint de contraseña** en la v1 (la pantalla Blazor era un mock). | E1 |
| POST | `/perfil/foto` | TODOS | Valida extensión (no magic bytes) y guarda la clave opaca en `persona.foto_clave`. | E1 |

## E2 — Reportes al propietario

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/captaciones/{idCaptacion}/reportes-propietario` | TODOS | ADMIN el tenant, BROKER el equipo vigente, AGENTE lo suyo. Un id de otra organización responde 404. | E2 |
| GET | `/captaciones/{idCaptacion}/reportes-propietario/preview` | TODOS | Mismo alcance; los tres agregados se calculan en SQL por captación, periodo y organización. | E2 |
| POST | `/captaciones/{idCaptacion}/reportes-propietario` | AGENTE | **Solo el agente responsable** registra. Ignora consultas/visitas/objeciones si el cliente las manda: son autoritativas del servidor. Reinicia el reloj de 15 días de F7. {autoridad: Alcances.alcanza} | E2 |

## E4 — Dashboard, indicadores y seguimiento

| Método | Ruta | Roles | Alcance | Vertical |
|---|---|---|---|---|
| GET | `/dashboard` | TODOS | Alcance y `ambito` por rol. La bandeja embebida sale de `/tareas`, que es solo del AGENTE: para **BROKER y ADMIN viaja vacía, no es un 403**. | E4 |
| GET | `/indicadores/resumen` | TODOS | Alcance **solo por agente responsable**: la captación no amplía el de nadie aquí. Distinto del seguimiento — **no unificar** (D-E4-4). | E4 |
| GET | `/indicadores/avance` | TODOS | RF-017, acumulado (no acepta periodo). Mismo alcance por agente responsable. | E4 |
| GET | `/indicadores/metas` | TODOS | Cada rol ve un alcance distinto: el AGENTE **las suyas**, el BROKER las de su equipo, el ADMIN las de su organización. Incluye **las no fijadas, con valor nulo** —ver a quién le falta es lo que deja al equipo sin semáforo—, la propuesta viva y el historial de revisiones. | E2.6 |
| PUT | `/indicadores/metas` | BROKER | Fija o revisa metas; idempotente por (agente, KPI, mes) y **con motivo obligatorio**, que queda en la serie. **El ADMIN no entra**: administrar usuarios no es dirigir producción, y un administrador que además dirija comercialmente lo hace con su rol de broker. No existe meta de equipo: es la suma de las de sus agentes (D-E2-2 §5). Fijar la de un agente fuera del alcance es regla de negocio, no 403. {autoridad: MetaComercialServiceImpl.exigirBroker} | E2.6 |
| POST | `/indicadores/metas/propuestas` | AGENTE | El agente **propone** un ajuste de su propia meta, con motivo. No la cambia: queda en espera. Una sola propuesta viva por KPI y mes; la siguiente reemplaza a la anterior. {autoridad: Alcances.de} | E2.6 |
| GET | `/indicadores/metas/propuestas` | BROKER, TENANT_ADMIN | Lo que espera decisión sobre los agentes del alcance. El ADMIN lee porque gobierno sin visibilidad no puede responder por qué un equipo está en rojo. {autoridad: Alcances.de} | E2.6 |
| POST | `/indicadores/metas/propuestas/{id}/decision` | BROKER | Acepta o rechaza. **Motivo obligatorio en los dos casos**: un «no» sin explicación deja al agente sin nada que aprender y un hueco en el historial. Decidir sobre un agente fuera del alcance es regla de negocio, no 403. {autoridad: MetaComercialServiceImpl.exigirBroker} | E2.6 |
| GET | `/seguimiento-comercial` | TODOS | Alcance por la **unión** de agente propio y agente de la captación; esa segunda rama existe **solo para el BROKER** (al AGENTE no le suma nada). Las filas **sin fecha encabezan** la lista. | E4 |

---

## Lo que esta tabla deja a la vista

Cinco cosas que sorprenden y **no** son errores; están fijadas por test y replicadas del cable v1:

1. **Cuatro pares de reglas de alcance que no se unifican.** Oportunidades y visitas alcanzan por
   *captación*; interacciones y solicitudes, por *agente supervisado*; contratos, otra vez por
   *captación*; indicadores solo por *agente responsable* y seguimiento por la *unión*. Cada
   intento de unificarlas rompe paridad.
2. **Los dos endpoints de comisión son `BROKER` sin `ADMIN`.** Funciona porque el filtro JWT
   publica una sola authority.
3. **`/tareas` es el único recurso sin acceso de ADMIN**, y es coherente con lo que es: una lista
   de cosas por hacer, no un tablero.
4. **Dos GET escriben**: `/alertas` y `/tareas` reconcilian antes de devolver. Es la forma de tener
   la bandeja al día sin planificador.
5. **`POST /oportunidades/{id}/cierre-exitoso` responde 400 para siempre.** El cierre exitoso lo
   produce la cascada del contrato, no un botón — el SPA no debe ofrecerlo.

## Qué NO cubre

- **Reportes PDF**: fuera del alcance de la migración (D-F5-1, ver
  `decision-reportes-pdf-fuera-de-alcance.md`). Los 5 endpoints `/pdf` de la v1 no tienen fila
  porque no existen en la v2.
- **Reglas de estado**: la tabla dice quién puede llamar y qué ve, no desde qué estado. Las
  máquinas de estado están en los contratos congelados por vertical.
- **RLS**: hoy el aislamiento de tenant es discriminador + filtro en la aplicación (D-24). Cuando
  se active RLS en Postgres, esta tabla no cambia: cambia dónde se aplica.
